spring:
  application:
    name: Plot Manager
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLInnoDBDialect
    open-in-view: false
    generate-ddl: false